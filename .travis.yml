language: python

sudo: required
dist: trusty

python:
  - "2.7"
  - "3.5"

git:
  submodules: false

addons:
  apt:
    packages:
      - gdal-bin
      - libgdal-dev
      - libgdal1h
      - libgdal1-dev
      - libgeos-dev
      - devscripts
      - debhelper
      - python-setuptools

# Handle Git submodules yourself
git:
    submodules: false

install:
  - izolation install izolation --upgrade
  - izolation install .
  - izolation install -r requirements-gdal.txt
  - izolation install -r requirements-dev.txt
  - izolation install coveralls

script:
  - python -m unittest tests
  - coverage run --source=pywps -m unittest tests
  - flake8 pywps/

after_success:
  - coveralls
  - debuild -b -uc -us

# whitelist
branches:
  only:
    - master

notifications:
  irc:
    channels:
      - "irc.freenode.org#geopython"
