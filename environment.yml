name: pywps
channels:
 - conda-forge
dependencies:
 - python >=3.10,<3.14
 - pip >=25.0
 - owslib >=0.35.0
 - requests >=2.32.5
 - werkzeug >=3.1.4
 - sqlalchemy >=2.0.44
 - lxml >=6.0.2
 - urllib3 >=2.5.0
 - markupsafe >=3.0.3
 - numpy >=1.22.2
 - zarr <3
 - fiona
 - geotiff
 # tests
 - pytest
 - ruff >=0.5.7
 # docs
 - sphinx >=7.0.0
 
